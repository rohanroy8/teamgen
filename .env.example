# Phase 0: DATABASE_URL for docker-compose db (user teamgen / db teamgen)
# Copy to .env for local dev. NEVER commit .env.
DATABASE_URL=postgresql://teamgen:teamgen@localhost:5432/teamgen
JWT_SECRET=change-me-dev-only-min-32-chars
NEXTAUTH_SECRET=change-me-dev-only-min-32-chars
NEXTAUTH_URL=http://localhost:3000
LLM_KEY=
EMBEDDING_MODE=bge-small
